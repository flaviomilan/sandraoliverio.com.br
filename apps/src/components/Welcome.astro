<div class="welcome-section">
  <div class="welcome-content">
    <div class="welcome-text">
      <span class="welcome-eyebrow">Bem-vindo</span>
      <h2>Sandra Oliverio <span class="accent-text">Assessoria</span></h2>
      <div class="decorative-line"></div>
      <p class="elegant-text">
        Com mais de 15 anos de experiência, criamos eventos memoráveis que refletem a personalidade
        e os desejos dos nossos clientes. Nossa paixão é transformar momentos especiais em
        experiências inesquecíveis.
      </p>
      <p>
        Cada evento é único e merece uma abordagem personalizada. Por isso, oferecemos serviços
        exclusivos que atendem às necessidades específicas de cada cliente, garantindo que cada
        detalhe seja cuidadosamente planejado e executado com excelência.
      </p>
      <div class="photo-container">
        <img src="/images/logo.png" alt="Sandra Oliverio" class="founder-photo" />
        <div class="founder-info">
          <span class="name">Sandra Oliverio</span>
          <span class="position">Fundadora & Assessora</span>
        </div>
      </div>
    </div>
    <div class="welcome-image">
      <div class="image-frame">
        <img src="/images/sandra-oliverio.jpg" alt="Sandra Oliverio" />
        <div class="frame-border"></div>
      </div>
      <div class="experience-badge">
        <span class="years">15+</span>
        <span class="text">Anos de Experiência</span>
      </div>
    </div>
  </div>
</div>

<style>
  .welcome-section {
    padding: 80px 0;
    overflow: hidden;
    background-color: var(--color-off-white);
  }

  .welcome-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .welcome-text {
    padding-right: 20px;
    position: relative;
  }

  .welcome-eyebrow {
    display: inline-block;
    font-family: var(--font-secondary);
    text-transform: uppercase;
    letter-spacing: 3px;
    font-size: 0.85rem;
    color: var(--color-dark);
    margin-bottom: 15px;
    position: relative;
    padding-left: 30px;
  }

  .welcome-eyebrow::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    width: 20px;
    height: 1px;
    background-color: var(--color-dark);
  }

  .welcome-text h2 {
    font-family: var(--font-primary);
    font-size: 2.8rem;
    font-weight: 300;
    line-height: 1.2;
    margin-bottom: 20px;
    letter-spacing: 1px;
  }

  .accent-text {
    color: var(--color-taupe);
    font-style: italic;
  }

  .decorative-line {
    width: 60px;
    height: 1px;
    background-color: var(--color-taupe);
    margin-bottom: 25px;
  }

  .elegant-text {
    font-family: var(--font-primary);
    font-size: 1.15rem;
    line-height: 1.7;
    color: var(--color-taupe);
    font-style: italic;
    margin-bottom: 20px;
  }

  .welcome-text p {
    margin-bottom: 20px;
    font-weight: 300;
    line-height: 1.8;
    color: var(--color-text);
  }

  .photo-container {
    margin-top: 40px;
    display: flex;
    align-items: center;
  }

  .founder-photo {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    margin-right: 20px;
    object-fit: cover;
  }

  .founder-info {
    display: flex;
    flex-direction: column;
  }

  .name {
    font-family: var(--font-script);
    font-size: 2.2rem;
    color: var(--color-dark);
    margin-bottom: 5px;
    transform: rotate(-3deg);
  }

  .position {
    font-family: var(--font-secondary);
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--color-text);
    display: block;
  }

  .welcome-image {
    position: relative;
  }

  .image-frame {
    position: relative;
    overflow: hidden;
    width: 100%;
    box-shadow: var(--shadow-medium);
    border-radius: 3px;
    overflow: hidden;
  }

  .image-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.5s ease;
  }

  .welcome-image:hover .image-frame img {
    transform: scale(1.03);
  }

  .frame-border {
    position: absolute;
    top: 15px;
    left: 15px;
    right: 15px;
    bottom: 15px;
    border: 1px solid rgba(255, 255, 255, 0.3);
    pointer-events: none;
  }

  .experience-badge {
    position: absolute;
    bottom: -20px;
    right: -20px;
    background-color: var(--color-button);
    color: white;
    width: 120px;
    height: 120px;
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-medium);
    z-index: 10;
  }

  .experience-badge .years {
    font-family: var(--font-primary);
    font-size: 2.5rem;
    font-weight: 400;
    line-height: 1;
  }

  .experience-badge .text {
    font-family: var(--font-secondary);
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 1px;
    text-align: center;
    max-width: 80%;
    line-height: 1.2;
  }

  @media (max-width: 992px) {
    .welcome-content {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .welcome-text {
      padding-right: 0;
    }

    .experience-badge {
      width: 100px;
      height: 100px;
      right: 0;
    }

    .experience-badge .years {
      font-size: 2rem;
    }

    .experience-badge .text {
      font-size: 0.65rem;
    }
  }
</style>
